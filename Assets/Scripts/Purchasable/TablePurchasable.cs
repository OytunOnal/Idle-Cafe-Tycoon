using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TablePurchasable : Purchasable
{
    // Start is called before the first frame update
    
    void Start()
    {
        price = 12;
        Init();
    }
}
